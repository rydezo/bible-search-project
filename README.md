Goal: Build a small data-science/search app that returns Bible verses matching a user's keyword query.
- Start simple (exact / boolean match)
- optionally add fuzzy matching and semantic (embedding) search
